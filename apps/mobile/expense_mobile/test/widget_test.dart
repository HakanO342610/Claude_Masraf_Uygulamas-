import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — full integration tests require a running backend.
    expect(true, isTrue);
  });
}
